server:
  http_addr: ":50000"
  grpc_addr: ":50001"

db:
  host: localhost
  port: "5432"
  database: wishlister
  user: wishlister

paths:
  migrations: "./db/init/init.sql"
  refresh_data: "./db/refresh_test_data.sql"
